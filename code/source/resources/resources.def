#ifdef HEADERS

#include "visual/texture.h"
#include "visual/mesh.h"
#include "visual/model.h"

#else

RESOURCE(Texture, init_Texture, deinit_Texture)
RESOURCE(Mesh, NULL, NULL)
RESOURCE(Model, NULL, NULL)

#endif
